export default function DemoBanner() {
  return (
    <div className="demo-banner">
      Demo mode: no OpenAI API key configured. Showing deterministic mock ad copy and a
      generated SVG preview instead of real AI output.
    </div>
  );
}
